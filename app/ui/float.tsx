import React from "react";

export default function Float({ children }: { children: React.ReactNode }) {
    // TODO add close button and close handler
    return <div className="float">{children}</div>;
}
